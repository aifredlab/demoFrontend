// project import
import chatList from './chatList';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [chatList]
};

export default menuItems;
